# Dependencies for the analysis pipeline (run_all.sh).
# Python 3.10+ recommended. All estimators are pure numpy/pandas — no scipy,
# statsmodels, or R are required (the DiD, BJS imputation, wild-cluster
# bootstrap, TOST, BH correction, and Markov tests are reimplemented in numpy
# for determinism and portability).
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
